<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Material - <%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css"> <!-- Adicionando seu arquivo CSS -->
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Editar Material</h2>
        <form action="/materiais/<%= material._id %>?_method=PUT" method="POST" class="bg-light p-4 rounded shadow">
            <div class="mb-3">
                <label for="nome" class="form-label">Nome do Material</label>
                <input type="text" name="nome" class="form-control" value="<%= material.nome %>" required>
            </div>
            <div class="mb-3">
                <label for="descricao" class="form-label">Descrição</label>
                <input type="text" name="descricao" class="form-control" value="<%= material.descricao %>" required>
            </div>
            <div class="mb-3">
                <label for="quantidade" class="form-label">Quantidade</label>
                <input type="number" name="quantidade" class="form-control" value="<%= material.quantidade %>" required>
            </div>
            <div class="mb-3">
                <label for="danca" class="form-label">Dança</label>
                <input type="text" name="danca" class="form-control" value="<%= material.danca %>" required>
            </div>
            <button type="submit" class="btn btn-success w-100">Salvar Alterações</button>
        </form>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
